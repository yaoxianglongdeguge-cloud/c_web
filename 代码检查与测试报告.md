# c_web —— 代码检查与测试报告（2026-08-27）

> 本文档记录第二轮代码审查 + 自动化测试 + valgrind 内存检查的结果。
> **未修改项目内任何源代码**；测试文件全部新建于 `test/src/`，产物在 `test/bin/`。
> 参考：`项目结构详解.md`（架构文档）、git log（已修复问题见文末）。

---

## 1. 测试基础设施

| 测试 | 覆盖组件 | 结果 |
|---|---|---|
| `test/src/test_memory_pool.c` | 内存池分配/释放/扩容/复用 | 🔴 3 项失败（扩容损坏） |
| `test/src/test_rwlock.c` | 读写锁互斥性 | 🔴 1 项失败（写读共存） |
| `test/src/test_hash.c` | hash_1/2/3 插入/查找/扩容 | ✅ 全部通过 |
| `test/src/test_http_ed_store.c` | 暂存区读/切分/残留/扩容 | 🔴 4 项失败 + **段错误** |
| `test/src/test_queue.c` | 三个队列（含多线程） | ✅ 全部通过 |
| `test/src/test_server.c` | **真实 Web_Driver 集成测试** | 🔴 4 项失败 + **2 个崩溃向量** |
| `test/src/debug_hash.c` | hash 调试辅助 | 临时工具 |

编译方式（不污染项目 Makefile，`test/src/*.c` 不在 Makefile 通配范围内）：
```bash
gcc -g -O0 test/src/test_xxx.c <依赖的模块.c> -o test/bin/test_xxx -lpthread
# 集成测试: 只对 main.c 用 -Dmain=web_driver_main 避免 main 冲突, 再与全部模块 + test_server.c 链接
```

---

## 2. 已确认 Bug（本轮测试 + valgrind 双重证实）

### 🔴 BUG-1　管道化请求丢失 —— `http_analysis/http_main.c:24,89`

```c
int r0=Http_ed_store_write(...);
int serial=fd_ob->ser_fina_send;      // L24: 循环外只取一次
while(1) {
    ...
    fd_ob->ser_fina_send++;           // L87: 每请求递增
    worker_to_profession(..., serial);// L89: 却一直用同一个 serial!
}
```

- **现象**：同一 read 批次的 N 个请求拿到**相同 serial**，全部落到发送槽 `serial%100` 同一个槽位，
  后到的响应覆盖先到的 → 只有最后一个响应被发出，其余请求丢失，客户端挂起。
- **实测**：3 个管道化请求只收到 **1** 个响应。
- **关联**：git log 中 "wrk 压测出问题，性能极差，几乎没有成功返回" 正是此 bug 的表现
  （wrk 默认 keep-alive 管道化）。
- **修复方向**：把 `serial` 的取值移入循环内（每次发任务前取 `ser_fina_send`）。

### 🔴 BUG-2　>2KB 请求导致进程崩溃 —— `http_analysis/http_ed_store.c:75`

```c
int Http_ed_store_expend(...) {
    int expendsize=nowsize*2/1024+1;          // KB
    Memory_Pool_alloc(pool,expendsize,&ptr);  // 分配 4KB
    ...
    h->end=h->begin+expendsize-1;             // BUG: 缺 *1024, end=begin+3!
    h->ptr_e=h->begin+ptr_e;                  // ptr_e=begin+2047 > end
}
```

- 对比 `Http_ed_store_alloc` 的 `h->end=h->begin+(size*1024)-1`，expend 少了 `*1024`。
- 扩容后 `end-begin=3`（应为 4095），`ptr_e(2047) > end(3)` → 下次
  `Http_ed_store_write` 中 `char C[length]` 的 `length=end-ptr_e` 为**负数** → VLA 负大小 → **SIGSEGV**。
- **实测**：单元测试打印 `end-begin=3`；集成测试 3000 字节请求 → 子进程信号 11；
  valgrind 定位 `http_ed_store.c:87`（Http_ed_store_write）。
- **影响**：任何 >2KB 的请求（大 POST、长头）都会崩掉整个服务器进程。

### 🔴 BUG-3　重复请求头导致进程崩溃 —— `http_analysis/http_analysis.c` + `data_struct/hash_2.c`

- 请求头重复时 `Hash2_Insert` 返回 -1（把"已存在"当失败），`Http_analysis_head` 返回 -1，
  **但 `error_reason` 未设置（保持 200）** → `deal_and_pack` 继续组装 Request，
  此时 `h->Url==NULL` → `Handler_Find(NULL)` → `hash(NULL)` 解引用 → **SIGSEGV**。
- **valgrind 调用栈**：
  `deal_task → Handler_Find → Hash_Find → bucket_site → hash(hash_1.c:277)`，`Address 0x0`。
- **实测**：`GET / HTTP/1.1\r\nHost: a\r\nHost: b\r\n\r\n` → 进程信号 11。
- 附带发现：HTTP 允许重复头（如多个 Cookie / X-Forwarded-For），按重复键视为错误是设计缺陷。

### 🔴 BUG-4　空闲连接永不超时 + 忙转 —— `my_thread/worker_thread.c`

```c
while(1) {
    int n=-1;
    while(1) {
        n=epoll_wait(w->epfd,events,1024,50);
        int e2=send_main(w,ed_store_blocknum);
        if(n>0) break;                 // 没事件就一直转
    }
    timer_overtime(w->my_timer,time,w); // 只在有事件后才执行!
    ...
}
```

- `timer_overtime` 在**内层循环外**，只有 `n>0`（有新事件）才能执行。
- 空闲连接（连上不发数据 / 发半包后停住）**永远不会被 408 超时**；
  且无事件时内层 `epoll_wait(50ms)` 循环空转。
- **实测**：空闲连接测试等待 2s+ 无 408。
- （注：commit 1eb2391 已把 timer_overtime 从"每轮 epoll 超时都调"改为"有事件才调"，虽然降低了调用频率，
  但同时也把空闲超时彻底关掉了——这是该次修改引入的回归。）

### 🔴 BUG-5　内存池扩容数据损坏 + 越界指针 + 释放失败 —— `memory_pool/memory_pool.c`

```c
int Memory_Entry_expend(Memory_Entry* e) {
    ...
    char* strip=(char*)malloc(expend_num*strip_size*1024);
    memcpy(e->memory_strip,strip,total_size);   // L103 BUG: 方向反了!
    free(e->memory_strip);                      // 旧块被释放, 旧指针悬垂
    e->memory_strip=strip;                      // 新块是未初始化垃圾
    ...
}
```

| 子问题 | 位置 | 后果 |
|---|---|---|
| `memcpy` 源/目标写反 | `Memory_Entry_expend` L103 | 把未初始化新内存拷入旧块后释放 → 旧分配全部损坏/use-after-free |
| `alloc2` 忽略栈取空返回值 | `Memory_Entry_alloc2` | 池满但 `haded_num<strip_num`（扩容后失衡时）返回 `memory_strip-1024` 越界指针且 `notfull=1` |
| `free` 范围检查拒绝旧指针 | `Memory_Pool_free` | 扩容前分配的块释放失败（返回 0）→ 条计数失衡、泄漏 |

- **实测**：`test_memory_pool` 扩容后 a/b 数据损坏；valgrind 报 **Invalid read（use-after-free）**；
  释放旧块后 `haded_num` 不再归位，alloc2 提前判满。
- **影响**：并发分配量达到预分配条数（如 store_area 1KB 类 400 条）触发扩容后，已用内存全部不可信。

### 🔴 BUG-6　读写锁互斥被破坏 —— `my_lock/my_rwlock_t.c:17,45`

```c
int my_lock_rdlock(...) {
    while(rw->waiting_writers>0||rw->waiting_writers>0)  // L17: 两条件相同, 漏了 writers>0
        pthread_cond_wait(...);
    ...
}
...
else if(rw->writers=1)   // L45: 赋值而非比较(碰巧可用)
```

- 写锁持有时（`writers=1, waiting_writers=0`），读锁**仍能进入** → 读写同时临界 → 数据竞争。
- **实测**：`wrlock` 后再 `rdlock` 成功，`writers=1 && readers=1`。
- 该模块当前未被使用，但若启用即是隐患。

### 🔴 BUG-7　路由表扩容泄漏 —— `data_struct/hash_1.c Hash_Expend`

```c
free(*h);   // 只释放 Hash_map 结构体; h->Elem 数组 + 所有 Entry 节点全部泄漏
```

- **实测**：valgrind 显示每次扩容泄漏约 1.2KB+（Elem 数组 + 840 字节链节点）。
- 路由表是启动时静态注册、几乎不扩容，但大路由表时会泄漏。

---

## 3. 功能正常项（本轮测试证实）

- ✅ 单请求 GET → 200（响应体/自定义头正确）
- ✅ 未知路由 → 404
- ✅ Query 参数解析（`/echo?name=x`）
- ✅ 8KB 响应体 Content-Length 正确
- ✅ 半包请求（分片发送）暂存区正确保留残留数据并补全 —— **此前 `ptr_b==ptr_e` 的赋值 bug 修复有效**
- ✅ hash_1/2/3 插入/查找/扩容/删除正确（`strnstr` 边界、FIFO 队列、多线程队列同步均正常）
- ✅ 三个队列多线程压力测试（2 生产者 × 2 消费者 × 1000）无丢包

---

## 4. valgrind 汇总

| 目标 | 发现 |
|---|---|
| `test_memory_pool` | **Invalid read ×2（use-after-free）**；definitely lost 232B + 17.5KB 间接 |
| `test_hash` | hash_1 扩容泄漏（Elem+节点，每次 ~1.2KB）；hash_2 strdup 测试键未释放 |
| `test_queue` | 队列无 destroy API → 退出时结构体泄漏（设计如此，进程常驻） |
| `test_rwlock` | 0 错误 |
| `test_server`（集成） | 重复头崩溃栈：`hash_1.c:277`（NULL 解引用）；大请求崩溃栈：`http_ed_store.c:87`（VLA 负长度） |

---

## 5. 已修复问题（git log f37209a / 1eb2391，本轮已复核）

| 文件 | 修复 |
|---|---|
| `http_main.c` | `ptr_b=ptr_e` 赋值 → `==` 比较 ✅ |
| `send_main.c` | `next=next+1%blocknum` → `next=(next+1)%blocknum` ✅ |
| `hash_2.c` | `Hash2_Insert` 先存节点再推进 ptr（消除空洞）✅ |
| `http_ed_store.c` | `Http_ed_store_free` 归还 size 补 `+1` ✅ |
| `http_state.c` | Content-Length 前导空格跳过循环修正 ✅ |
| `worker_thread.c` | `timer_overtime` 移出内层忙循环（但见 BUG-4 回归）⚠️ |
| `profession_thread.c` / `send_main.c` / `http_state.c`（f37209a） | http_get 分配大小、错误包 size_resp=0、body 长度解析 ✅ |

---

## 6. 遗留风险与建议（优先级排序）

1. **P0**：修 BUG-1（serial 移入循环）—— 否则 wrk/浏览器 keep-alive 压测必然大量丢包。
2. **P0**：修 BUG-2（expend 补 `*1024`）—— 否则任何 >2KB 请求直接崩溃进程。
3. **P0**：修 BUG-3（重复头判错/校验 `h->Url`）—— 可被单条 HTTP 请求远程打崩。
4. **P1**：修 BUG-4（`timer_overtime` 放回每轮循环内）—— 恢复空闲超时与 408。
5. **P1**：修 BUG-5（memcpy 方向、alloc2 返回值检查、free 范围校验放行扩容前指针）。
6. **P1**：修 BUG-6（rdlock 条件补 `writers>0`）。
7. **P2**：hash_1 扩容内存回收（提供 free 接口或一次性全量释放）。

---


---

## 7. 复测结果（用户修复后，2026-08-27 晚）

用户对 6 个文件做了修改（未提交），随后全量复测。**测试均在 /tmp/cwfix 副本中进行**（复制源码 + 最小修正），
工作区文件保持未动。结果如下：

### 7.1 用户修改 + 最小编译修正后（修复前状态）

| 测试 | 结果 |
|---|---|
| `test_rwlock` / `test_queue` / `test_http_ed_store` | ✅ 通过（serial++、expend\*1024、rwlock 修复生效） |
| `test_server` 管道化 | ✅ 3 个响应全部有序返回（BUG-1 修复） |
| `test_server` >2KB 大请求 | ✅ 不再崩溃（BUG-2 修复） |
| `test_hash` | 🔴 **`free(): invalid pointer` 崩溃**（hash_1 清理循环 bug） |
| `test_server` 超时 408 | 🔴 仍失败（timer 自比较 bug，用户未改 timer.c） |
| `test_server` 重复请求头 | 🔴 仍崩溃（用户未改 http_analysis.c） |
| `test_memory_pool` | 🔴 仍 3 失败（扩容悬垂指针） |

### 7.2 用户修改中存在的 2 个编译错误（当前无法 `make`）

```c
// memory_pool/memory_pool.c:104 —— 类型不匹配
Memory_Entry* p=e->memory_strip;          // memory_strip 是 char*
// 应为:
char* p=e->memory_strip;

// data_struct/hash_1.c:208 —— 取结构体当指针
Entry* m=(*h)->Elem[i];                   // Elem[i] 是 Entry 值
// 应为:
Entry* m=&((*h)->Elem[i]);
```

### 7.3 用户 hash_1 清理循环本身有 3 处错误（即使修好类型也崩）

```c
for(int i=0;i<PRIME_BUCKET_SIZES[PRIME_BUCKET_SIZES_nidex-1];i++)  // ① 只遍历7桶, 应为 (*h)->bu_num
{
    Entry* m=&((*h)->Elem[i]);
    while(m->next!=NULL)
    {
        Entry* p=m->next;
        free(m);          // ② free() Elem 数组内部元素 = 非法指针, 直接崩
        m=p;
    }
}
free(*h);                 // ③ Elem 数组本体从未释放(还会泄漏)
```

**正确写法**（已在 /tmp 验证，valgrind 无泄漏）：
```c
for(int i=0;i<(*h)->bu_num;i++) {
    Entry* m=(*h)->Elem[i].next;      /* 跳过数组槽(头部) */
    while(m!=NULL) { Entry* p=m->next; free(m); m=p; }
}
free((*h)->Elem);                     /* 整个数组一次释放 */
free(*h);
```

### 7.4 复测中新确认的 bug（用户未覆盖到，均在 /tmp 验证修复）

**① timer.c:89,91 —— 超时自比较（永不超时）**
```c
time_t time=t1.time;                    // time=堆顶自身时间戳
while(... && time-(t->q->queue[1].time)>overtime)  // 差恒为0
// 改为: time_t now=time(NULL); 比较 now-queue[1].time
```

**② timer 陈旧项崩溃** —— `fd_close` 从不把 fd 从定时器堆移除；
超时触发后对已关闭 fd 调 `fd_close` → `Fd_Table_find` 返回 NULL → `fd_ob->http_store` 解引用 **SIGSEGV**
（valgrind 栈：`fd_close(connect_manage.c:49) ← timer_overtime`）。
修复：`timer_overtime` 在 `fd_close` 前先查 `Fd_Table_find`，不在表内则只弹堆。

**③ prior_queue_1 三处 bug**（小顶堆）：
- `prior_queue_1_down` **缺 return 语句**（函数尾部无返回，返回垃圾值，调用方误判 -1）
- `down` 内 `queue[t*2+1]` 在 `elem_end` 为偶数时**越界读**（右子节点不存在）
- `prior_queue_1_pop` 在 `elem_end==2` 分支**不递减 `queue[0].fd`**（计数漂移，堆清空后 `queue[-1]` 越界读）

**④ http_main.c:74 —— 请求副本分配截断（越界写+读）**
```c
Memory_Pool_alloc2(worker->http_pool,h_size/1024,&ptr,&notfull);
// h_size=1500 -> 1500/1024=1 -> 只分 1KB, 但 Http_ed_store_copy/Http_get 要读写 h_size 字节 -> OOB
// 改为: h_size/1024+1
```

**⑤ 重复请求头崩溃（BUG-3 根因补充）** —— 修复为解析失败且未设错误码时置 400：
```c
if(a1!=1) { if(*error_reason==200) *error_reason=400; return -1; }
```

### 7.5 修复全部应用后（/tmp 验证）的最终结果

```
单元测试 : memory_pool 3失败(设计问题, 见下)  其余4个全部通过
集成测试 : 全部通过 (0 失败)  — 200/404/query/8KB/管道化3响应/半包/408超时/重复头/大请求
valgrind : 0 处 Invalid read / 0 处 SIGSEGV   (集成测试全程)
```

### 7.6 唯一剩余问题：内存池扩容设计缺陷（P1，建议重构）

即使 memcpy 方向修正，`Memory_Entry_expend` 扩容时仍会 **free 旧块**，使所有未归还的旧指针悬垂：
- valgrind 仍报：`Invalid read ... Address is 1024 bytes inside a block of size 2,048 free'd`
- `Memory_Pool_free` 的范围检查会拒绝归还扩容前的旧指针（`return 0`），条计数失衡、alloc2 提前判满
- 触发条件：某大小类并发占用超过预分配条数（http_pool 400 / store_area 400 / txt_pool 200）
- **建议**：扩容时保留旧块（如按块链表管理，free 时按指针查所属块），而非单一连续大块迁移；
  或在压测规模内把预分配条数调大到不会触发的程度

### 7.7 给用户的可执行修复清单（按文件）

| 文件 | 修改 |
|---|---|
| `memory_pool.c` | ① L104 类型 `char* p`；②（建议）扩容不再 free 旧块（重构） |
| `hash_1.c` | ① L208 `&Elem[i]`；② 清理循环改为遍历 `bu_num` 释放节点 + 整块 Elem |
| `timer.c` | ① 自比较改 `time(NULL)`；② 加陈旧 fd 守卫 |
| `prior_queue_1.c` | ① down 补 return 1 + 修复右子越界；② pop 的 elem_end==2 分支补 `queue[0].fd--` |
| `http_main.c` | L74 `h_size/1024+1` |
| `http_analysis.c` | 解析失败未设错误码时兜底置 400 |



---

## 8. 第 2 轮复测结果（用户修复 9 个文件后，2026-08-27 深夜）

### 8.1 你的修改中仍有的编译错误（当前 `make` 仍过不了）

| 文件 | 错误 | 修正 |
|---|---|---|
| `hash_1.c:208` | `Entry* m=(*h)->Elem[i];` 类型不匹配（Elem[i] 是值） | `Entry* m=(*h)->Elem[i].next;` 或 `&Elem[i]` |
| `main.c:129` | `m1->w` 对结构体用 `->` | `m1.w` / `m1.fd` / `m1.time` / `m1.ed_store_blocknum` |
| `main.c:69` | `pthread_t tid2` 声明被移进注释块，外面还在用 | 补回 `pthread_t tid2;` |
| `timer.c:90` | `time_t now=time(NULL);` 中 `time` 被局部变量遮蔽 | 删掉 `time_t time=t1.time;`，直接比较 `now-t1.time` |

> 这 4 处就是"自比较 bug"里提到的变量遮蔽问题 + 重构时的残留，修完才能编译。

### 8.2 你已修好的部分（编译修正后 /tmp 验证 ✅）

- `http_analysis.c` 5 处 `*error_reason=400` → **重复请求头不再崩溃** ✅
- `timer.c` 自比较改 `now-time`（思路正确，但见上表遮蔽问题）✅
- `http_ed_store.c` expend `*1024`、`memory_pool.c` memcpy 方向、`rwlock` 两处、`serial++` ✅

### 8.3 编译修正后仍存在的崩溃（valgrind 实锤）

**`timer_overtime` 缺"陈旧 fd 守卫"** —— 超时测试必崩：
```
Invalid read of size 8  at fd_close (connect_manage.c:49)
  by timer_overtime (timer.c:93)   ← Address 0x10 (fd_ob==NULL)
```
根因：`fd_close` 不把 fd 从定时器堆移除 → 已关闭连接的陈旧项残留 → 超时触发时
`Fd_Table_find` 失败 → `fd_ob==NULL` → 解引用崩溃。
**修复**（在 /tmp 已验证）：`timer_overtime` 循环里先查表，不在表内只弹堆不关连接。

### 8.4 补齐全部修复后（/tmp 验证）的最终结果

```
单元测试 : memory_pool 3失败(内存池扩容设计问题, 见 §7.6)  其余4个全部通过
集成测试 : 全部通过 (0 失败)  — 200/404/query/8KB/管道化/半包/408/重复头/大请求
valgrind : 0 Invalid read / 0 Invalid write / 0 SIGSEGV
```

### 8.5 本轮最终结论与待办（按优先级）

1. **P0 编译**：修 §8.1 的 4 处编译错误
2. **P0 崩溃**：`timer_overtime` 加陈旧 fd 守卫（§8.3）——否则开启超时后关闭过的连接一多必崩
3. **P1**：`prior_queue_1.c` 三处（down 缺 return / 偶数堆右子越界 / pop 计数不递减）——修复见 §7.4③，timer 路径依赖它
4. **P1**：`http_main.c` `h_size/1024+1`（请求副本分配截断 OOB）
5. **P1**：内存池扩容设计（§7.6）——并发超过 400/500 时旧指针悬垂
6. **P2**：hash_1 清理循环——你新版的 `m->next` 逻辑会漏掉每个桶**最后一个节点**（单节点桶整个漏），用 §7.3 正确写法

---

*测试代码在 `test/src/`，可随时重跑；本报告仅记录，未改动任何生产源码。*



---

## 9. 第 3 轮复测结果（用户再次修复后，2026-08-27 深夜）

### 9.1 本轮你的改动与验证

| 你的改动 | 结果 |
|---|---|
| `timer.c` 自比较改 `now-t1.time` + 删局部变量 `time` | ✅ 思路正确（但残留 L105 `time=t1.time;` 导致编译错误） |
| `timer.c` 新增 `time_free()` | ⚠️ 思路正确但**未接入 `fd_close`**，且自身缺少 `elem_end--`/计数递减 |
| `hash_1.c` 改用 `Elem[i].next` | ✅ 编译通过；⚠️ 但 `while(m->next!=NULL)` 仍**漏每个桶尾节点** |
| `main.c` `m1.w` | ✅ 编译通过 |
| `http_analysis.c` 400 / expend `*1024` / serial++ / rwlock | ✅ 验证通过（同前） |

### 9.2 仍有的编译错误（当前 `make` 过不了）

1. **`main.c:69`** —— `pthread_t tid2;` 声明仍被注释在 `/* */` 块里，L69 还在用 → `tid2 undeclared`
2. **`timer.c:105`** —— `time=t1.time;` 残留（`time` 局部变量已删），对函数名赋值 → `lvalue required`

### 9.3 编译修正后的实测（保持你的代码思路，仅编译修正）

- 超时 408 **这次通过了** ✅，但随后仍 **SIGSEGV**：
  ```
  Invalid read at fd_close (connect_manage.c:49) ← timer_overtime   fd_ob==NULL
  ```
- 根因：`time_free` 写好了但**没被调用**——`fd_close` 不移除定时器堆项，陈旧 fd 仍会在超时触发时被 `fd_close(NULL)` 崩溃。

### 9.4 正确接入方式（/tmp 已验证）

1. **`timer.h`** 补声明：`int time_free(timer* t,int fd);`
2. **`time_free` 修正**（原版缺堆大小/计数维护）：
   ```c
   t->q->elem_end--;      // 堆大小-1 (原版没有)
   t->q->queue[0].fd--;   // 节点计数-1 (原版没有)
   if(i < t->q->elem_end) // 被删的不是堆尾才搬
   { t->q->queue[i] = t->q->queue[t->q->elem_end];
     prior_queue_1_down(t->q,i); }
   ```
3. **`fd_close` 接入**：`send_tool_destory(...)` 后加一行
   `time_free(w->my_timer, client_fd);`

### 9.5 补齐全部修复后（/tmp 最终验证）

```
单元测试 : memory_pool 3失败(内存池扩容设计问题, 见 §7.6)  其余4个全部通过
集成测试 : 全部通过 (0 失败) — 200/404/query/8KB/管道化/半包/408/重复头/大请求
valgrind : 0 Invalid read / 0 Invalid write / 0 SIGSEGV
```

### 9.6 当前剩余待办（优先级排序）

1. **P0 编译**：修 §9.2 两处编译错误
2. **P0 崩溃**：按 §9.4 修正并接入 `time_free`（你已写对大半）
3. **P1**：`http_main.c:74` 改 `h_size/1024+1`（大请求越界写/读，valgrind 实锤）
4. **P1**：`prior_queue_1.c` 三处（down 缺 return / 偶数堆 `queue[t*2+1]` 越界 / pop 计数不递减）
5. **P1**：内存池扩容设计（§7.6）
6. **P2**：hash_1 清理改 `while(m!=NULL)`（现版本漏每桶尾节点）

---

*测试代码在 `test/src/`，可随时重跑；本报告仅记录，未改动任何生产源码。*

---

## 10. 第 4 轮复测结果（用户大重构后，2026-08-28 凌晨）—— ✅ 首次全绿

### 10.1 本轮你的改动（18 个文件）

- ✅ **memory_pool 重构**：`Memory_Pool_alloc`/`alloc2` 合并为带 `notfull` 的 `Memory_Pool_alloc`，
  **放弃强制扩容**（池满返回 `notfull=0` 由调用方降级 503）→ 彻底规避"扩容悬垂指针"问题
- ✅ **prior_queue_1**：`down` 加右子边界判断 + 补 `return 1`；`pop` 的 `elem_end==2` 分支补计数递减
- ✅ **timer_free 接入**：`timer.h` 声明 + `connect_manage.c` 在 `fd_close` 里调用（**修复陈旧 fd 崩溃**）
- ✅ `http_main.c` `h_size/1024+1`（修大请求越界）、`Http_ed_store_alloc/expend` 失败检查、
  各调用点适配新内存池 API、`Http_get` 按完整条大小归还、send_tool 分配失败处理

### 10.2 你工作区当前仍有 3 处需修（测试在 /tmp 修正后全绿）

| 位置 | 问题 | 修正 |
|---|---|---|
| `timer.c:136` | `t->q->queue[0].fd--` **漏分号** | 补 `;` |
| `connect_manage.c:72` | `timer_free()` **漏参数和分号** | `timer_free(w->my_timer, client_fd);` |
| `send_tool.c:19` | `if(notfull==1) return -1;` **判断反了**（notfull==1 是成功） | 应为 `if(notfull==0) return -1;` |

> ⚠️ 特别是 `send_tool.c`：当前逻辑下**每个连接的第一个响应都会因"成功却返回失败"而触发 503 关闭**，
> 实测全部请求 503 + 连锁崩溃。这是本轮新引入的最严重问题。

### 10.3 /tmp 修正这 3 处后的最终验证

```
单元测试 : 5/5 全部通过 (memory_pool 新语义/rwlock/hash/暂存区/队列)
集成测试 : 全部通过 (0 失败) — 200/404/query/8KB/管道化3响应/半包/408/重复头/>2KB
valgrind : 0 Invalid read / 0 Invalid write / 0 SIGSEGV   (全程, 含子进程)
```

### 10.4 结论

- **架构层面的两个顽疾已解决**：内存池扩容悬垂（改为非扩容+降级）、定时器陈旧 fd 崩溃（time_free 接入）。
- 剩余的都是**一行级的收尾修正**（3 处，见 §10.2），修完即可 `make && make run`。
- `test_memory_pool.c` 已同步为**新 API 语义**（非扩容/池满降级/超大 malloc 兜底/数据完整性）。

---

*测试代码在 `test/src/`，可随时重跑；本报告仅记录，未改动任何生产源码。*


---

## 11. 云服务器大文件传输卡死专项（2026-08-28）

> 背景：本地秒发 10MB 图片正常，云服务器上"传输不过来卡住"甚至"进程卡死"。
> 本文档记录了定位过程、根因、验证与修复建议。

### 11.1 现象

- 本地（loopback）：大文件 0.05s 内发完，一切正常。
- 云服务器：大图片发一半卡住；严重时整个进程假死（其他请求也无响应）。

### 11.2 问题一：大文件发送被空闲超时掐断（✅ 已修复）

**根因**：`send_tool/send_main.c` 大文件是**多轮 sendfile**（发不完就重推回队列），但
文件未发完的两个分支（`sent>0` / `EAGAIN`）都 `return 1` 提前退出，**跳过了末尾的
`timer_alloc_and_reset`** → 该连接计时器停留在 accept 时刻 → 1 秒空闲超时触发 → `fd_close(408)`
掐断连接。

**为何本地正常、云服务器卡**：本地 10MB < 1 秒发完，计时器来不及触发；云服务器 RTT 大、
TCP 慢启动，10MB 要几秒~几十秒 → 必被 408 掐断。**之前"能发"是因为 timer 自比较 bug
（`time - queue[1].time` 恒 0）导致超时永不触发；修好自比较后问题暴露。**

**修复（已加入 `send_main.c`，2 行）**：

```c
if(sent > 0) {
    Send_thing_queue_push(...);
    timer_alloc_and_reset(w->my_timer, s.fd, w);   // ✅ 已加
    return 1;
}
else if (sent < 0 && errno == EAGAIN) {
    Send_thing_queue_push(...);
    timer_alloc_and_reset(w->my_timer, s.fd, w);   // ✅ 已加
    return 1;
}
```

**验证**：长驻服务器 + `time=1`（最短超时）+ 限速 500KB/s 下载 10MB（20 秒）→ **完整下载，
md5 与源文件一致**。

### 11.3 问题二：队列阻塞级联 → 进程假死风险（🔴 未修复，P0）

**机制（死锁链）**：

```
云服务器连接慢 → worker 发送吞吐低
  → Send_thing_queue(容量100) 持续满
  → profession 阻塞在 Send_thing_queue_push 的 sem_wait（queue/send_thing_queue.c）
  → profession 停止消费 Task_Queue → Task_Queue(容量100) 满
  → worker 阻塞在 Task_queue_push 的 sem_wait（queue/task_queue.c）→ 停止 epoll
  → 所有连接无响应 → 进程假死
```

**压测观察**（15 路限速大文件 + 200 并发小请求）：

```
worker 线程    : ep_poll（正常）
profession 线程: futex_do_wait（阻塞在信号量！）
```

**架构原则违反**：**事件循环线程（worker）绝不能阻塞在队列上**；当前 `Task_queue_push` /
`Send_thing_queue_push` 都是阻塞式 `sem_wait`，队列满即卡死调用线程。

### 11.4 问题三：send_main 发送吞吐瓶颈（🔴 未修复，P1）

`send_main` 每 50ms（epoll 超时）才被调用一次，大文件事件 `sendfile` 部分发送后即
`return 1`（一次调用只处理 1 个事件）→ **worker 发送吞吐 ≈ 20 事件/秒**。云服务器慢速连接
一多，发送事件积压，队列长期满。

### 11.5 问题四：Send_thing_queue pop 无条件 sem_post（🔴 未修复，P1）

`Send_thing_queue_top_and_pop` 在**队列空时也 `sem_post(notfull)`** → 信号量膨胀，
容量保护失真（发送队列的"满"约束实际失效）。`Task_queue` 的 pop 配对正确（仅真出队时 post），
可对照修正。

### 11.6 问题五：write 部分写入不处理（🔴 未修复，P1）

`send_main.c` L90：`write(send_fd, ptr, len)` 返回 `n < len` 或 `EAGAIN` 时**不处理**，
直接释放响应内存继续 → 云服务器上 socket 缓冲区满时，内存响应体发送不完整 → 客户端卡住。
应参照文件重推机制：部分写入时把剩余部分重推。

### 11.7 验证结果汇总（本轮）

| 场景 | 结果 |
|---|---|
| 10MB @ 500KB/s（time=1，含 §11.2 修复） | ✅ 完整下载，md5 一致 |
| 100MB @ 5MB/s（time=60） | ✅ 完整下载，md5 一致 |
| 300 并发小请求（本地） | ✅ 全 200，无假死 |
| 15 路限速大文件 + 200 并发（本地压测） | ⚠️ profession 阻塞 futex，但 worker 未卡死（本地快） |

### 11.8 修复建议（优先级）

| 优先级 | 修改 | 说明 |
|---|---|---|
| **P0** | `Task_queue_push` / `Send_thing_queue_push` 改**非阻塞/超时**（队列满降级 503，而非 sem_wait） | 根治进程假死；事件循环线程绝不阻塞 |
| **P0** | 队列容量调大（100 → 1024+） | 缓解积压 |
| **P1** | `receive_and_send_main` 无事件时连续调用 `send_main` 直到队列空 | 提升发送吞吐 |
| **P1** | `Send_thing_queue_top_and_pop` 仅真出队时 `sem_post` | 修复信号量膨胀 |
| **P1** | `write` 部分写入重推剩余 | 修复响应不完整 |


---

## 12. 云服务器大文件传输修复记录（2026-08-28）

> 现象演化：本地秒发 → 云服务器"发一半卡住 + 进程卡死" → 修复后**云服务器向网页稳定发送大文件（不卡不崩）**，
> 剩余问题：**传输偏慢**（见 §12.4）。

### 12.1 已修复问题与方案（用户实施）

| # | 问题 | 根因 | 修复方案 | 状态 |
|---|---|---|---|---|
| 1 | 大文件发一半被掐断 | `send_main` 文件未发完分支 `return 1` 跳过末尾 `timer_alloc_and_reset` → 1 秒空闲超时 408 | 各提前 `return` 分支补 `timer_alloc_and_reset`（commit 4c035f0） | ✅ |
| 2 | 大响应体传输中断 | `write()` 返回 `EAGAIN(n<0)` 无分支处理 → 响应体丢失、客户端等不满 Content-Length | `send_main` 补 `else if(n==-1 && errno==EAGAIN)` 重推分支 | ✅ |
| 3 | 响应体内存释放大小错乱 | `else if(n>=len)` 里先 `size_resp=0` 再 `Memory_Queue_push` → 释放大小记录为 0 | 先 push 原值再清零 | ✅ |
| 4 | 响应头混入 body（内容错位） | `send_thing_queue.c` pop 笔误 `s.offset=...off`（应为 `s.off=...off`）→ 部分写入重推后 `off` 丢失、从头部重发 | 修正为 `s.off=sq->queue[sq->ptr_out].off` | ✅ |
| 5 | 发送队列容量保护失真 | `Send_thing_queue_top_and_pop` 空队列也 `sem_post` → 信号量膨胀 | 仅真出队时 `sem_post`；返回改为 `return s`（修正编译错误） | ✅ |
| 6 | 大文件分段传输计时 | 分段发送期间连接计时器被反复重置 | `send_main` 各发送分支统一 `timer_alloc_and_reset` | ✅ |

**验证**（云服务器实测）：大文件从云服务器向网页发送**稳定**，不再卡住、不再崩溃。

### 12.2 支持项

- `Send_tq_Entry` / `Send_tool_Entry` 增加 `off` 字段，区分"内存响应体偏移"与"文件偏移"，支持内存响应体部分写入续传。
- `Http_ed_store_write` 优化：去掉中间缓冲，`read` 直接写入暂存区（消除 VLA 负长度隐患）。
- 超时参数 `time` 调为 10 秒（`main.c`），平衡空闲回收与慢速传输。

### 12.3 遗留 bug 观察（未修复，供后续）

- **`Send_thing_queue_top_and_pop` 无条件 `sem_post` 已修**，但 `Task_queue` 的 `push`/`pop` 仍是阻塞式 `sem_wait`——队列满时 worker/profession 可能级联阻塞（§11.3 的进程假死风险仍在）。
- `send_main` 一次只处理 1 个发送事件（大文件 `sendfile` 部分发送后 `return 1`）。

### 12.4 待解决：传输偏慢（用户反馈）

**现象**：稳定了，但大文件传输速度偏慢。

**根因**：`send_main` 每 50ms（epoll 超时）才被调用一次，每次只发 1 个 TCP 窗口的数据 →
吞吐 ≈ **TCP 窗口大小 / 50ms**。云服务器上窗口小（cwnd 几 KB~几十 KB）、RTT 大 → 吞吐被锁死。

**候选方案**（已在 /tmp 验证性实现）：
- **方案 A（推荐）**：`send_main` 发送遇 `EAGAIN`/部分发送时注册 `EPOLLOUT`，socket 可写（客户端 ACK 腾出窗口）时立即续发，不再等满 50ms。代码见 §12.5。
- **方案 B**：worker 循环里连续调用 `send_main` 直到发送队列清空（提高单次 epoll 周期的发送量）。

### 12.5 EPOLLOUT 方案代码（/tmp 已验证编译）

```c
/* send_tool/send_main.c 顶部增加辅助函数 */
static void send_set_epollout(worker* w,int fd) {
    struct epoll_event ev;
    ev.events=EPOLLIN|EPOLLOUT;
    ev.data.fd=fd;
    epoll_ctl(w->epfd,EPOLL_CTL_MOD,fd,&ev);
}
static void send_clear_epollout(worker* w,int fd) {
    struct epoll_event ev;
    ev.events=EPOLLIN;
    ev.data.fd=fd;
    epoll_ctl(w->epfd,EPOLL_CTL_MOD,fd,&ev);
}
/* 4 个"未发完"分支（write 部分发送 / write EAGAIN / sendfile 部分发送 / sendfile EAGAIN）
   在 push 回队列 + timer_alloc_and_reset 之后、return 1 之前，各加一行：*/
send_set_epollout(w, send_fd);
/* 发送完成（while 循环结束）时，移除 EPOLLOUT 防 level-triggered 空转：*/
send_clear_epollout(w, send_fd);
```

